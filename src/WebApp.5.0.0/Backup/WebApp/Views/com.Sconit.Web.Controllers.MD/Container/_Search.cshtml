@using com.Sconit.Entity.MD
@using com.Sconit.Entity.SYS
@using com.Sconit.Web.Models.SearchModels.MD
<fieldset>
    @using (Html.BeginForm("List", "Container"))
    {   
        <div class="search">
            <div class="search-label">
                <label for="Container_Code">
                    @Resources.MD.Container.Container_Code
                </label>
            </div>
            <div class="search-field">
                @Html.TextBox("Code", TempData["ContainerSearchModel"] != null ? ((ContainerSearchModel)TempData["ContainerSearchModel"]).Code : string.Empty)
            </div>
             <div class="search-label">
                <label for="Container_Description">
                    @Resources.MD.Container.Container_Description
                </label>
            </div>
            <div class="search-field">
                @Html.TextBox("Description", TempData["ContainerSearchModel"] != null ? ((ContainerSearchModel)TempData["ContainerSearchModel"]).Description : string.Empty)
            </div>
        </div>
        <div class="search-footer">
         @Html.Hidden("isFromList", true)
            @*<button type="submit" id="btnSearch">
                @Resources.SYS.Global.Button_Search</button>
            <button type="button" id="btnNew" onclick="location.href='@Url.Content("~/Container/New")'">
                @Resources.SYS.Global.Button_New</button>*@
        
        @Html.Button(Resources.SYS.Global.Button_Search, "Url_Container_View", new Dictionary<string, string>()
	{
	    {"type", "submit"},
	    {"id", "btnSearch"}
	})
            @Html.Button(Resources.SYS.Global.Button_New, "Url_Container_Edit", new Dictionary<string, string>()
	{
	    {"type", "button"},
	    {"id", "btnNew"},
        {"onclick","location.href='"+@Url.Content("~/Container/New")+"'"}
	})</div>

    }
</fieldset>
