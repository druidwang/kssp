@using com.Sconit.Web.Models.SearchModels.TMS

<fieldset>
    @using (Html.BeginForm("List", "Carrier"))
    {   
        <div class="search">
            <div class="search-label">
                <label for="Carrier_Code">
                    @Resources.MD.Party.Party_Carrier_Code
                </label>
            </div>
            <div class="search-field">
                @Html.TextBox("Code", TempData["CarrierSearchModel"] != null ? ((CarrierSearchModel)TempData["CarrierSearchModel"]).Code : string.Empty)
            </div>
            <div class="search-label">
                <label for="Carrier_Name">
                    @Resources.MD.Party.Party_Carrier_Name
                </label>
            </div>
            <div class="search-field">
                @Html.TextBox("Name", TempData["CarrierSearchModel"] != null ? ((CarrierSearchModel)TempData["CarrierSearchModel"]).Name : string.Empty)
            </div>
        </div>
        <div class="search-footer">
            @Html.Hidden("isFromList", true)
            <button type="submit" id="btnSearch">
                @Resources.SYS.Global.Button_Search</button>
            <button type="submit" id="btnExport">
                @Resources.SYS.Global.Button_Export</button>
            @Html.Button(Resources.SYS.Global.Button_New, "Url_Carrier_Edit", new Dictionary<string, string>()
	        {
	            {"type", "button"},
	            {"id", "btnNew"},
                {"onclick","location.href='"+Url.Content("~/Carrier/New/") +"'"}
	        })

        </div>

    }
</fieldset>
<script type="text/javascript">
    var t = "";
    $('#btnSearch').click(function () {

        if (t != "") {
            $('form').attr('action', t);
        }
        $('form').submit();
    });
    $('#btnExport').click(function () {

        if (t == "") {
            t = $('form').attr('action');
        }
        $('form').attr('action', "/Carrier/Export/");
        $('form').submit();
    });
</script>