@model System.Web.Mvc.SelectList
@{
    string name = ViewBag.ControlName;
    string id = string.IsNullOrWhiteSpace(ViewBag.ControlId) ? name : ViewBag.ControlId;
    bool enable = ViewBag.Enable != null ? ViewBag.Enable : true;


}
@(Html.Telerik().ComboBox()
        .Name(name)
                .HiddenInputHtmlAttributes(new { id = id })
            .DataBinding(binding => binding.Ajax().Select("_AjaxLoadingTaskSubType", "Common"))
        .ClientEvents(events => events

                                .OnDataBinding("TaskSubType_DataBinding")
        )
        .BindTo(Model)
        .Enable(enable)
             .AutoFill(true)
)
