@using com.Sconit.Entity.MD
@using com.Sconit.Web.Models.SearchModels.MD
<fieldset>
    @using (Html.BeginForm("List", "Currency"))
    {   
        <div class="search">
            <div class="search-label">
                <label for="Currency_Code">
                    @Resources.MD.Currency.Currency_Code
                </label>
            </div>
            <div class="search-field">
                @Html.TextBox("Code", TempData["CurrencySearchModel"] != null ? ((CurrencySearchModel)TempData["CurrencySearchModel"]).Code : string.Empty)
            </div>
             <div class="search-label">
                <label for="Currency_Name">
                    @Resources.MD.Currency.Currency_Name
                </label>
            </div>
            <div class="search-field">
                @Html.TextBox("Name", TempData["CurrencySearchModel"] != null ? ((CurrencySearchModel)TempData["CurrencySearchModel"]).Name : string.Empty)
            </div>
             

        </div>
        <div class="search-footer">
         @Html.Hidden("isFromList", true)
            @*<button type="submit" id="btnSearch">
                @Resources.SYS.Global.Button_Search</button>
            <button type="button" id="btnNew" onclick="location.href='@Url.Content("~/Currency/New")'">
                @Resources.SYS.Global.Button_New</button>*@
                @Html.Button(Resources.SYS.Global.Button_Search, "Url_Currency_View", new Dictionary<string, string>()
	{
	    {"type", "submit"},
	    {"id", "btnSearch"}
	})
            @Html.Button(Resources.SYS.Global.Button_New, "Url_Currency_Edit", new Dictionary<string, string>()
	{
	    {"type", "button"},
	    {"id", "btnNew"},
        {"onclick","location.href='"+@Url.Content("~/Currency/New")+"'"}
	})
        </div>

    }
</fieldset>
