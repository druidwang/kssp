@using com.Sconit.Entity.ISI
@using com.Sconit.Web.Models.SearchModels.ISI
<fieldset>
    @using (Html.BeginForm("List", "TaskMaster"))
    {
        <div class="search">
            <div class="search-label">
                <label for="Code">
                    @Resources.ISI.TaskMaster.TaskMaster_Code
                </label>
            </div>
            <div class="search-field">
                @Html.TextBox("Code", TempData["TaskMasterSearchModel"] != null ? ((TaskMasterSearchModel)TempData["TaskMasterSearchModel"]).Code : string.Empty)
            </div>

            <div class="search-label">
                <label for="Description">
                    @Resources.ISI.TaskMaster.TaskMaster_Description1
                </label>
            </div>
            <div class="search-field">
                @Html.TextBox("Description", TempData["TaskMasterSearchModel"] != null ? ((TaskMasterSearchModel)TempData["TaskMasterSearchModel"]).Description : string.Empty)
            </div>
            <div class="search-label">
            </div>
            <div class="search-field">
            </div>
        </div>
        <div class="search-footer">
            @Html.Hidden("isFromList", true)
            <button type="submit" id="btnSearch">
                @Resources.SYS.Global.Button_Search
            </button>
            <button type="button" id="btnNew" onclick="location.href='@Url.Content("~/TaskMaster/New")'">
                @Resources.SYS.Global.Button_New
            </button>
        </div>
    }
</fieldset>
