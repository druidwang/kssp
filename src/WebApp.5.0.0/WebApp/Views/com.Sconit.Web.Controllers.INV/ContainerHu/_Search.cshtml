
@using com.Sconit.Web.Models.SearchModels.INV
<fieldset>
    @using (Html.BeginForm("List", "ContainerHu"))
    {   
        <div class="search">
            <div class="search-label">
                <label for="Container_Code">
                    @Resources.MD.Container.Container_Code
                </label>
            </div>
            <div class="search-field">
                @Html.TextBox("Code", TempData["ContainerHuSearchModel"] != null ? ((ContainerHuSearchModel)TempData["ContainerHuSearchModel"]).ContainerId : string.Empty)
            </div>
             <div class="search-label">
                <label for="Container_Description">
                    @Resources.MD.Container.Container_Description
                </label>
            </div>
            <div class="search-field">
                @Html.TextBox("Description", TempData["ContainerHuSearchModel"] != null ? ((ContainerHuSearchModel)TempData["ContainerHuSearchModel"]).Container : string.Empty)
            </div>
        </div>
        <div class="search-footer">
         @Html.Hidden("isFromList", true)
            @*<button type="submit" id="btnSearch">
                @Resources.SYS.Global.Button_Search</button>
            <button type="button" id="btnNew" onclick="location.href='@Url.Content("~/Container/New")'">
                @Resources.SYS.Global.Button_New</button>*@
        
        @Html.Button(Resources.SYS.Global.Button_Search, "Url_ContainerHu_View", new Dictionary<string, string>()
	{
	    {"type", "submit"},
	    {"id", "btnSearch"}
	})
            @Html.Button(Resources.SYS.Global.Button_New, "Url_ContainerHu_View", new Dictionary<string, string>()
	{
	    {"type", "button"},
	    {"id", "btnNew"},
        {"onclick","location.href='"+@Url.Content("~/ContainerHu/New")+"'"}
	})</div>

    }
</fieldset>
